[Masscan]
# masscan scan rate in packets per second
rate = 1000

[Utils]
# endpoints used by get_own_ip to discover the host's public IP
api_list = ["https://api.seeip.org/","https://api.ipify.org/"]

[Scan]
# ip_addresses / crawl_ports / excl_ports are injected by
# ReconService.download_repo() at deploy time. Kept here so the section
# always exists and recon can be run standalone for testing.
